#include "main.h"

/**
 * _abs - Computes the absolute value of an integer.
 * @b: The number to be computed.
 * Return: Absolutr value of a number or zero
 */
int _abs(int b)
{
	if (b >= 0)
	{
	return (b);
	}
	return (-b);
}
